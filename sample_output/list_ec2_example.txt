EC2 (no filter) — 0 found:
------------------------------------------------------------------------------

================================================================================
INFRASTRUCTURE EXPLANATION (GROUP 13 - KICKSSHOES PROJECT)
================================================================================

* Why are 0 EC2 instances found?
  - Group 13 deployed the KicksShoes application using Amazon ECS (Elastic Container Service) with AWS Fargate as the underlying launch type.
  - AWS Fargate is a serverless compute engine for containers. It abstracts away the underlying infrastructure, eliminating the need to provision, configure, or maintain traditional EC2 virtual machines.
  - As a result, standard EC2-focused CLI queries correctly return 0 active instances. This output validates our architectural design, confirming that the environment is fully serverless and operationally optimized.
