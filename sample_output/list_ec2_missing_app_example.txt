EC2 (no filter) — 0 found:
------------------------------------------------------------------------------

================================================================================
INFRASTRUCTURE EXPLANATION - KICKSSHOES PROJECT
================================================================================

* Why are 0 EC2 instances found?
  - I deployed the KicksShoes application using Amazon ECS (Elastic Container Service) with AWS Fargate as the underlying launch type.
  - Because AWS Fargate is a serverless compute engine for containers, it abstracts away the infrastructure. This means I don't need to manually provision, configure, or maintain any traditional EC2 virtual machines.
  - As a result, running standard EC2-focused CLI queries correctly returns 0 active instances. This output verifies my architectural setup, confirming that the environment is fully serverless and operationally optimized.
